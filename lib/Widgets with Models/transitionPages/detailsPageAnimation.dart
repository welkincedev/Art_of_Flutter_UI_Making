
import 'package:flutter/material.dart';

class DetailsPageAnimation extends StatelessWidget {
  final String title;
  const DetailsPageAnimation({super.key, required this.title});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}

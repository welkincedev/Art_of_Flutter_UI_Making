import 'package:flutter/material.dart';

class TabScn2 extends StatelessWidget {
  const TabScn2({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Card(child: Text("HI Guys"),),
    );
  }
}

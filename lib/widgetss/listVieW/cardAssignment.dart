import 'package:flutter/material.dart';

class CardAssginmet extends StatelessWidget {
  const CardAssginmet({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Card(
        color: Colors.green[900],

      ),
    );
  }
}
